package com.academy.ferdowsi.training.core.api;


public class ApiMethod {

    /*ApiInterface mApiAll;

    public ApiMethod() {
        mApiAll = MyApplication.getApiComponent().getApiAll();
    }

    //region Auth

    public void authMobile(String mobile, int countryId, BaseObserver<BaseApiModel> observer) {
        mApiAll.authMobile(mobile, countryId)
                .subscribeOn(Schedulers.io())
                .observeOn(AndroidSchedulers.mainThread())
                .subscribe(observer);
    }
 */

}